## Example environment variables for local development and homologation
# Choose which app to validate: APP_ENV=dev (local) or APP_ENV=hom (homologation)
APP_ENV=dev

# Local Streamlit app URL (used when APP_ENV=dev)
LOCAL_URL=http://localhost:8501

# Homologation / staging app URL (used when APP_ENV=hom)
HOM_URL=https://autocoinbot.fly.dev/

# Optional: explicit APP URL to override APP_ENV
# APP_URL=https://your.custom.url/

# Other env vars used across project (examples)
TRADES_DB=trades.db
API_KEY=
API_SECRET=
API_PASSPHRASE=

# Show browser during Selenium runs (set to 1 to see the GUI)
SHOW_BROWSER=0